# INITIAL_ADMIN_PASSWORD: initial password for the seeded Super Admin
# account (EVL-ADMIN-001) used by the /admin panel's backend API
# (server.ts). Only needed if you run the admin server (`npm run dev:admin`).
INITIAL_ADMIN_PASSWORD="EVLab@2026!Admin"
